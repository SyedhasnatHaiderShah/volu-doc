# 01 · Monorepo Structure

**Status:** 🟢 Approved

How Volu's code is organised across repositories.

---

## Repository topology

We use **multiple repositories**, not a single mega-monorepo. Two reasons:
- Mobile (Flutter) and backend (TypeScript) have very different toolchains; mixing them adds friction.
- Boundary discipline: contracts via OpenAPI + generated clients beat shared types in a monorepo.

```
volu-backend/      — NestJS backend + admin BFF
volu-admin/        — Next.js admin console (consumes volu-backend)
volu-mobile/       — Flutter monorepo with user app + merchant app + shared packages
volu-infra/        — Terraform + scripts + DR runbooks
volu-design/       — Figma exports, brand assets, content
volu-docs/         — This documentation (you are here)
```

---

## `volu-backend`

```
volu-backend/
├── package.json
├── tsconfig.json
├── nest-cli.json
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── src/
│   ├── main.ts
│   ├── app.module.ts
│   ├── modules/
│   │   ├── identity/
│   │   ├── catalog/
│   │   ├── commerce/
│   │   ├── payments/
│   │   ├── coupons/
│   │   ├── redemption/
│   │   ├── wallet/
│   │   ├── payouts/
│   │   ├── refunds/
│   │   ├── raffles/
│   │   ├── loyalty/
│   │   ├── reviews/
│   │   ├── notifications/
│   │   ├── ads/
│   │   ├── invoicing/
│   │   ├── audit/
│   │   └── reporting/
│   ├── shared/
│   ├── workers/
│   └── webhooks/
├── test/
│   ├── e2e/
│   └── fixtures/
├── docker-compose.dev.yml
└── Dockerfile
```

Module boundaries enforced by ESLint rules: a module cannot import from another module's `repositories/`, `domain/`, or `services/` directly — only from its `index.ts` public exports.

---

## `volu-admin`

```
volu-admin/
├── package.json
├── next.config.mjs
├── tailwind.config.ts
├── src/
│   ├── app/                # Next.js App Router
│   ├── components/
│   ├── lib/
│   ├── hooks/
│   └── styles/
├── public/
└── tests/
    ├── unit/
    └── e2e/                # Playwright
```

Generated TypeScript SDK (from volu-backend's OpenAPI) sits at `src/lib/api/generated/`. Regenerated by `pnpm gen:api` → invokes `openapi-typescript` against the spec URL.

---

## `volu-mobile`

```
volu-mobile/
├── melos.yaml
├── pubspec.yaml             # workspace root
├── apps/
│   ├── user_app/
│   │   ├── lib/
│   │   │   ├── main.dart
│   │   │   ├── app.dart
│   │   │   ├── core/
│   │   │   ├── features/
│   │   │   └── shared/
│   │   ├── ios/
│   │   ├── android/
│   │   └── pubspec.yaml
│   └── merchant_app/
│       └── (same structure)
│
├── packages/
│   ├── volu_core/           # shared models, utilities, types
│   ├── volu_design_system/  # tokens + components
│   ├── volu_api_client/     # generated from OpenAPI
│   ├── volu_auth/           # auth flows shared between apps
│   ├── volu_payments/       # Stripe/Tabby/Tamara integrations
│   ├── volu_localization/   # EN + AR translations
│   ├── volu_analytics/      # PostHog + Mixpanel + Sentry wrappers
│   └── volu_diagnostics/
│
├── tools/
│   ├── codegen/             # API client codegen
│   └── icon_assets/
│
└── ci/
    └── fastlane/
```

Melos commands:
- `melos bootstrap` — link
- `melos run analyze`, `test`, `format-check`, `gen:api`
- `melos run build:user:android`, `build:user:ios`, `build:merchant:android`, `build:merchant:ios`

---

## `volu-infra`

```
volu-infra/
├── README.md
├── terraform/
│   ├── envs/
│   │   ├── prod/
│   │   ├── staging/
│   │   └── dr/
│   └── modules/
│       ├── network/
│       ├── ecs-service/
│       ├── rds/
│       ├── elasticache/
│       ├── secrets/
│       └── observability/
├── scripts/
│   ├── deploy.sh
│   ├── rollback.sh
│   ├── dr-failover.sh
│   └── restore-snapshot.sh
└── runbooks/
```

Terraform state in S3 with DynamoDB lock; encrypted; versioned. Per-environment workspace.

---

## `volu-docs` (this repo)

```
volu-docs/
├── README.md
├── docs/                    (the structure you're reading)
└── adr/                     (Architecture Decision Records)
    ├── 0001-modular-monolith.md
    ├── 0002-flutter-for-mobile.md
    ├── ...
```

ADR format: short, dated, with context + decision + consequences.

---

## Versioning

- Backend, admin, mobile have independent semver versions.
- API spec versioned via URL path (`/api/v1`).
- Generated SDKs versioned per backend release; consumers pin to a known-good version.

---

## CI/CD per repo

| Repo | Trigger | What |
|---|---|---|
| `volu-backend` | PR | Lint, type, test, build, scan |
| `volu-backend` | merge `main` | Stage → prod deploy |
| `volu-admin` | PR | Lint, type, test, build |
| `volu-admin` | merge `main` | Vercel auto-deploy |
| `volu-mobile` | PR | Lint, analyze, test, build smoke |
| `volu-mobile` | tag | Mobile release pipeline |
| `volu-infra` | PR | Terraform plan; cost diff |
| `volu-infra` | merge `main` | Apply (with manual gate for prod) |
| `volu-docs` | PR | Markdown lint + link check |

---

## See also

- [Coding Standards](./02-coding-standards.md)
- [Git Workflow](./04-git-workflow.md)
- [CI/CD](../07-infrastructure/04-ci-cd.md)
